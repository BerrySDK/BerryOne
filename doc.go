// Package berryone provides the Go edition of the BerryProtocol developer experience.
//
// This repository is designed to mirror the public ergonomics of the TypeScript SDK:
// connection helpers, event subscriptions, session persistence, and rich message
// payloads. The current implementation ships with an in-memory transport so the public
// API is immediately testable while the real WhatsApp Web transport is being ported.
package berryone
